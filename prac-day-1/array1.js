// target: display full array
let myLearnings = ['variable', 'let', 'var', 'const', 'array', 'conditional', 'loops', 'index'];

for (let i = 0; i < myLearnings.length; i++) {
    let learnings = myLearnings[i];
    console.log(learnings);
}