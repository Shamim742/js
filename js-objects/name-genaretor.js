// write a function that has 2 parameters firstName and lastName and that return fullName
function fullName (firstName, lastName) {
    let fullName = firstName + " " + lastName;
    return fullName;
}
let firstName1 = 'Md. Shamim';
let lastName1 = 'Islam';

let fullName1 = fullName(firstName1, lastName1);
console.log(fullName1);