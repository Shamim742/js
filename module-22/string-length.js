const name = 'shamim islam';
const len = name.length;
console.log(len)

// strings are immutable (values aren't changing)
let num = (1234);
num[2] = 0;
console.log(num);

// str are immutable
const fristName = 'Hannan';
const lastName = 'Poribohon';

const fullName = fristName + lastName;
console.log(fullName);