// // roastGiven = roastGiven + 1;
/*
1.loop var
2.condition inside while
3.loop body
4.change the loop var if loop var stop then it is infinite variable
*/
let roastGiven = 0;

while (roastGiven < 7) {
    console.log('Give them some roast')
    roastGiven += 1;
    console.log(roastGiven)
}

let dueDate = 10;

while (dueDate < 21) {
    dueDate++;
    console.log('give me my money')
}